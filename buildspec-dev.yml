version: 0.2

phases:
  install:
    commands:
      - echo "=== Installing dependencies ==="
      - cd app && npm ci

  build:
    commands:
      - echo "=== Running tests ==="
      - npm test
      - echo "=== Tests passed ==="
